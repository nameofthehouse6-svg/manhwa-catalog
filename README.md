# manhwa-catalog

Curated catalog (`catalog.json`) for the reader app: title, source URL, type, chapter count, cover filename. Downloaded on-device; all browsing is local. Covers live in the `manhwa-covers` repo.
